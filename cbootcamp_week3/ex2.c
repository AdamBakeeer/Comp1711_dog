#include <stdio.h>
int main() {
    int a = 13;
    if (a%4 == 0 && a%5 == 0)
    {
    printf("Number is divisible by 4 and 5"/n);
    }
    else 
    {
        printf("Number is not divisible by 4 and 5"/n);
    }
    return 0;
}